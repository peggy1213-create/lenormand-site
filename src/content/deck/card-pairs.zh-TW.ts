// Hand-authored Traditional Chinese pair examples. Must carry the same
// (first, second) keys as card-pairs.en.ts once both are populated — never
// machine-translated, never copied from the English file. Empty for now.
import type { CardPair } from "./types";

export const CARD_PAIRS: CardPair[] = [];
